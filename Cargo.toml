[package]
name = "accord"
version = "0.1.0"
edition = "2021"
description = "Apply unified diffs to a working tree - the write-side counterpart to diff"
license = "MIT"
repository = "https://github.com/NefaroXX/accord"
homepage = "https://github.com/NefaroXX/accord"

[dependencies]
clap = { version = "4", features = ["derive"] }

[dev-dependencies]
tempfile = "3"
